#include <deque>
#include <iostream>
#include <vector>
#include <queue>
#include <random>
using namespace std;
typedef long long ll;
using pii = pair<int, int>;
using ipii = pair<int,pii>;
int inf = 1<<30;
int N_MAX=1000, P_MAX=10000, K_MAX=9999, I_MAX=1000000;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    random_device rd;
    mt19937 gen(rd());
    uniform_int_distribution<int> dis();
}